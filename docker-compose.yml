services:
  frontend:
    build: .
    container_name: coyote_frontend
    env_file:
      - .env
    ports:
      - "${FRONTEND_PORT:-80}:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
